mixin meme_item(meme)
    .meme__item(class!=meme.tags)
        img(src='/uploads/'+meme.id+'.jpg')
        .details
            span.tag!=meme.tags
            span.pp__counter(meme_id!=meme.id)
                i(class='fab fa-pied-piper-pp')
                .pp__amount
                    =meme.likes
            span.title
                !=meme.meme_title
            span.info
                ='Made by: '
                a(href=`/profile/${meme.author_id}`)
                    !=meme.author_username